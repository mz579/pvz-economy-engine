# PvZ Economy Engine V2.0 项目规则

## 版本边界

- 本项目统一按 V2.0 实现，忽略旧文档和旧代码中的 V3.0 目标。
- 主链固定为：北京示例数据或用户地区 CSV -> 清洗与特征 -> 植物映射 -> 可解释评分 -> 组合优化 -> Streamlit。
- 不引入 Airflow、多地区 SaaS、Alpha、夏普比率、复杂金融回测或不必要的预测模型。
- Docker 和短期价格预测均为可选项，不得阻塞核心链路。

## 分批执行

- 严格按既定批次修改，不把后续批次功能提前混入当前批次。
- 每批开始前列出预计修改的文件。
- 每批结束必须汇报修改文件、完成内容、验证方式、遗留问题和下一批计划。
- 不允许多个 agent 同时修改同一个文件，尤其是 `app.py`、`README.md`、`requirements.txt` 和配置文件。

## 工程规则

- `app.py` 只负责接收输入和展示结果，业务流程通过 `src/` 中的模块调用。
- 数据采集、清洗、特征、评分和优化保持单向依赖，不允许互相导入 UI。
- 文件路径应从项目根目录或集中配置解析，避免依赖启动时的当前目录。
- 新增依赖前先说明原因；优先使用 pandas、NumPy、requests、PuLP 和 Streamlit。
- 保证 PuLP 不可用或网络断开时仍有可运行的 fallback。
- 不继续扩展 `LEGACY.md` 中列出的旧功能。

## 数据规则

- V2.0 只维护北京新发地一个在线示例源，但允许用户在本地上传任意地区 CSV；不扩展为多地区爬虫或 SaaS。
- 原始数据保存在 `data/raw/`，包含来源、采集时间、原始单位和来源地址。
- 清洗数据保存在 `data/processed/`，价格统一为元/kg，日期统一为 YYYY-MM-DD。
- 网络失败时使用可追溯的本地 CSV 样例；不得伪装演示数据为实时数据。
- 不手工编辑 `data/processed/` 中的生成结果。

## 模型与 UI 规则

- 每个评分都必须能拆解为植物属性、价格系数、稳定性和阳光成本贡献。
- 优化结果必须附带约束检查和自然语言推荐理由。
- PvZ 风格使用 CSS、自绘、emoji 或允许再分发的素材，不使用官方版权资源。
- 优先保证信息清晰、布局稳定和可访问性，再增加视觉效果。

## 验证规则

- 每批至少运行与改动对应的 `unittest`、CLI smoke test 或 Streamlit 启动检查。
- 如果依赖、真实数据源、模型公式或 UI 方向存在实质不确定性，暂停并请求用户确认。
- 出现失败时先保留安全 fallback，不能让已有主流程完全不可运行。
