# 末日菜园作战室 V2.1.1

V2.1.1 当前稳定版说明。解压后按照 `README_FIRST.txt` 启动即可；本文件仍属于 V2 范围，不引入旧规划中的 3.0 内容。

## V2.1.1 发布口径修复

- CLI 版本说明改为从包版本读取，避免与页面、README 和 Release 显示不一致；
- 重新生成桌面端与移动端实际页面截图，确保版本号与当前代码一致。

## 阵容集中度修复

- 新增通用单植物数量上限：`max(1, ceil(草坪格子数 × 30%))`，统一适用于全部植物；
- 30% 是 V2 用于降低单一植物集中风险的可解释模型假设，不是现实验证出的最佳比例；
- PuLP 与 greedy fallback 共用同一上限计算规则，不针对小喷菇或零成本植物硬编码；
- 默认 150 阳光、20 格调整为 `土豆雷 ×5 + 小喷菇 ×6 + 灯笼草 ×1`，使用 150 阳光、占用 12 格，组合相对比较指数为 0.9186；
- 优化结果允许保留空格；剩余阳光不足或候选植物达到单植物上限时，空格不会被标记为错误或不可行；
- CLI、草坪展示、模型约束说明和调试元数据均会显示本轮 30% 规则及单植物数量上限。

## 作战室界面与解释收尾

- 配置区使用明确表单和唯一主按钮“🚀 开始分析”，参数未重新提交时保留上一轮结果；
- 首屏直接展示推荐植物、数量、真实菜价、阳光、格子和相对比较指数；
- 新增原创暗色“作战终端 → 本轮阵容 → 草坪部署 → 市场情报”视觉，未使用官方 PvZ 素材；
- 下半页按“推荐原因 → 市场证据 → 候选对比 → 模型细节”组织；
- 推荐原因压缩为价格、作战属性、阳光成本和入选依据四类事实；
- 市场区展示价格趋势及当前单价/样本均价对比，对空数据、单条数据和恒定价格提供文字状态；
- Streamlit/Vega 初始化交互范围时仍可能输出瞬时 `Infinite extent` / scale binding 控制台警告；已确认数据与图表正常，V2.1.1 不为消除无害提示重写图表栈；
- 完整排名、评分公式与权重、求解器状态、约束、映射和调试信息收进折叠标签页；
- 草坪格子明确标记为数量展示布局，不宣称是优化器输出坐标；
- 桌面端 1280×720 与移动端 390×844 已完成实际浏览器回归。

## Windows 启动修复

- 修复 `start_windows.bat` 在部分中文 Windows 环境中因 CMD 编码解析而直接退出的问题；
- 兼容检测 `py` / `python`，已安装的依赖不再每次重复安装；
- 启动失败时保留窗口，并生成 `startup.log`；
- 发布包中的批处理强制使用 Windows CRLF 换行。
- 跳过 Streamlit 首次运行的邮箱询问，并关闭匿名使用统计；
- 服务仅监听本机 `localhost`，启动后直接打开本地页面。

## V2.1.1 功能

- 北京新发地菜价作为默认示例和离线 fallback；
- 支持上传任意地区 CSV，并按当地菜价重新评分；
- 支持 UTF-8、GB18030、中文/英文表头、元/kg 和元/斤；
- 15 种植物映射、可解释末日性价比指数；
- PuLP 整数规划与贪心 fallback；
- PvZ 风格 Streamlit 页面、CSV 模板和本地启动脚本；
- 数据、评分、优化、UI 和端到端自动化测试。

## 注意

- 第一次启动需要联网安装 Python 依赖；之后可使用本地 CSV 离线运行。
- 本项目不包含官方 PvZ 图片、音效或其他游戏素材。
